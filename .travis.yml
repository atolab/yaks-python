sudo: false
language: python
python:
  - "3.5"
env:
  matrix:
  - TOXENV=py3
  - TOXENV=pep8

install:
  - travis_retry pip install tox
  - pip install codecov
script:
  - bash -ex .yaks-ci.sh
cache:
  directories:
    - $HOME/.cache/pip
after_success:
- codecov